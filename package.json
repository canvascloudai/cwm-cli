{
  "name": "@cwm/cli",
  "version": "1.0.0",
  "description": "Cloud World Model CLI — cwm command-line tool",
  "type": "module",
  "bin": {
    "cwm": "./dist/index.cjs"
  },
  "main": "./dist/index.cjs",
  "scripts": {
    "build": "cd .. && npm run build:cli",
    "build:watch": "tsc --watch"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
